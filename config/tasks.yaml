tasks:
  memory_cleanup:
    enabled: true
    script: "run_memory_cleanup.py"
    entry_point: "run_cleanup"
    trigger: "interval"
    trigger_args:
      hours: 12
    args:
      retention_days: 30
